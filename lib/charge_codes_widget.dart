import 'package:flutter/material.dart';


class ChargeCodesWidget extends StatelessWidget {
  const ChargeCodesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text("NOT IMPLEMENTED")
      ],
    );
  }
  
}